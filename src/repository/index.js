const UserRepository  = require('./UserRepository');
const PhoneRepository = require('./PhoneRepository');

const repositories = {
  UserRepository,
  PhoneRepository
}

module.exports = repositories;